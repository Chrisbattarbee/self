# Current Config Structure

```json
{
  "server_location": "http://self.X.com",
  "calories": {
    "when": "schedule.every(10).seconds.do(job)",
    "mfp_username": "X",
    "mfp_password": "Y",
  },
}

```